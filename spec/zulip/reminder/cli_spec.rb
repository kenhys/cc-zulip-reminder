RSpec.describe Zulip::Reminder::Cli do
  it "has a version number" do
    expect(Zulip::Reminder::Cli::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
